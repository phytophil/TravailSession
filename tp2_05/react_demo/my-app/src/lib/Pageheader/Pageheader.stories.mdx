import { Meta, Props, Canvas, Story } from '@storybook/addon-docs';
import Pageheader from './Pageheader';

<Meta title="Example/Pageheader" component={Pageheader} />

# Example Component Demo

## Pageheader props
<Props of={Pageheader} />

## Description

`Pageheader` component, from AntD, provides user basic interface that can be used to highlight the page topic, display important information about the page, and carry the action items related to the current page (including page-level operations, inter-page navigation, etc.) It can also be used as inter-page navigation.

## Demo

<Canvas>
  <Story name="AntD Pageheader">
    {(args) => <Pageheader {...args} />}
  </Story>
</Canvas>