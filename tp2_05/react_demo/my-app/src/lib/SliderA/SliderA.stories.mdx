import { Meta, Props, Canvas, Story } from '@storybook/addon-docs';
import SliderA from './SliderA';

<Meta title="Example/SliderA" component={SliderA} />

# Example Component Demo

## SliderA props
<Props of={SliderA} />

## Description

`SliderA` component provides user basic interface to create a simple slider with timing of scrolling to the next card/picture.

## Demo

<Canvas>
  <Story name="AntD SliderA">
    {(args) => <SliderA {...args} />}
  </Story>
</Canvas>