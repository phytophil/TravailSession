import { Meta, Props, Canvas, Story } from '@storybook/addon-docs';
import BadgeVisibility from './BadgeVisibility';

<Meta title="Example/BadgeVisibility" component={BadgeVisibility} />

# Example Component Demo

## BadgeVisibility props
<Props of={BadgeVisibility} />

## Description

`BadgeVisibility` component, from Material UI, provides user basic interface to increment BadgeVisibility by one.

## Demo

<Canvas>
  <Story name="Material UI BadgeVisibility">
    {(args) => <BadgeVisibility {...args} />}
  </Story>
</Canvas>