import { Meta, Props, Canvas, Story } from '@storybook/addon-docs';
import Counter from './Counter';

<Meta title="Example/Counter" component={Counter} />

# Example Component Demo

## Counter props
<Props of={Counter} />

## Description

`Counter` component provides user basic interface to increment counter by one.

La composante `Counter` est un compteur permettant d'incrémenter un nombre par un.
Elle provient de la librairie ant-D.

## Demo

<Canvas>
  <Story name="Example Counter">
    {(args) => <Counter {...args} />}
  </Story>
</Canvas>
